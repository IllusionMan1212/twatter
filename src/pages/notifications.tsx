import { Flex } from "@chakra-ui/react";
import { ReactElement } from "react";

export default function Notifications(): ReactElement {
    return (
        <Flex>Notifications coming soon</Flex>
    );
}
